# docs/notes/archive — 冷存储（非事实源）

promoted note 的原始稿冷存于此。**不是事实源**：权威内容以顶栏 `promoted →` 指针指向的
ADR / `docs/modules` / `agents/knowledges/` 文件为准，本目录仅保留实测证据、file:line 与原始措辞。

- 不进 AGENTS.md / INDEX / 热 grep 路径（已知 topic 从 active 检索，archive 不参与）。
- 由 `/opsx:triage` 定期按保留窗口清理（见 `agents/commands/opsx/triage.md`）。