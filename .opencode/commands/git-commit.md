---
description: Execute git commit with conventional commit message analysis, intelligent staging, message generation, and automatic push to remote repository
---

Run the git-commit skill: load `SKILL.md` from `.opencode/skills/git-commit/SKILL.md` and follow its workflow exactly (conventional commit type/scope detection, intelligent staging for logical grouping, message generation, push on request). Do not commit or push unless the user asks.